1. f(n) for printList is:
O(1) - > O(N)


2. If we are using an array to store our list then f(n) for findKth element is:
O(1)

3. If we are using a linked list  to store our list then f(n) for findKth element is:
O(1) - O(N)

4.int size () const function can be used with vector as well as list STLs.
T

5.void pop_fron() const function is available for vector STL.
F

6.Iterator can be used to print all elements of a list.
F. - > T

7. Iterator begin() function returns an iterator representing the                          item in the container.
First

8.Iterator end() function returns an iterator representing the                          item in the container.
endmarker

9.itr++ will move the iterator to the next element in the list.
T

10. The last item in a list is labeled An.
F